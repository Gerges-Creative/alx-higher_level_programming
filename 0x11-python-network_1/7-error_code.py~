#!/usr/bin/python3

from sys import argv
import requests

if __name__ == "__main__":
    r = requests.get(argv[1])

    if r.Response.raise_for_status() >= 400:
        print("Error code: {}".format(r.status_code))
    else:
        print(r.text)
