#!/usr/bin/python3

import sys
import urllib.request
import urllib.parse

req = urllib.request.Request(sys.argv[1])
with urllib.request.urlopen(req) as response:
    the_header = response.read().decode("utf-8")

print("{}".format(the_header.X-Request-Id))
